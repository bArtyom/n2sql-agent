# 开发约定

## 软件工程原则

- 先理解现有代码、需求和约束，再开始修改；不凭猜测大范围重构。
- 保持改动小而聚焦，避免将无关格式化、重命名或依赖升级混入同一改动。
- 代码应职责单一、边界清晰、易于测试；优先复用现有模式，避免不必要的抽象。
- Go 代码优先采用符合惯用 Go 的直白写法，面向 Go 初学者保持命名清楚、控制流易读、函数职责单一；不要为了减少文件或函数数量把 HTTP、业务、存储和调度逻辑混在一起。
- 独立频率或独立生命周期的后台工作应独立调度；例如高频任务轮询和低频清理使用清晰分开的 ticker、循环或 worker，不依赖隐含的时间判断耦合执行。
- 对外接口、配置、数据结构和错误处理保持一致；破坏性变更必须同步更新调用方与文档。
- 不在代码、日志、测试数据或文档中提交密码、令牌、连接串和其他秘密。
- 对外部输入进行校验，对权限、数据访问、文件操作和网络请求采用最小权限原则。
- 优先使用项目已有依赖和工具；新增依赖前确认其必要性、维护状态与安全性。

## 实施流程

1. 阅读相关代码、配置、测试和文档，明确改动范围。
2. 先实现最小可用改动，再补齐边界条件和错误处理。
3. 为新增或修改的行为补充相应测试；修复缺陷时优先增加能复现问题的测试。
4. 运行与改动匹配的格式化、静态检查、单元测试和必要的集成测试。
5. 交付时说明修改内容、验证结果及尚未验证的部分。

## 代码质量与测试

- 命名应准确表达意图；注释解释设计原因或非显然约束，不重复代码表面含义。
- 优先使用短小、可顺序阅读的函数和显式分支；复杂并发、channel、定时器和上下文控制必须用注释或文档说明执行时机、退出条件和职责边界。
- 面向学习交付时，除展示项目代码外，补充一个最小 Go 示例和从入口到调用链的解释，让代码结构和语法可以被初学者跟上。
- 错误必须被显式处理并保留有用上下文；不得静默吞掉错误。
- 测试应覆盖正常路径、边界输入和失败路径，避免依赖不稳定的时间、网络或外部状态。
- 修改用户界面或交互流程时，进行真实浏览器验证并保留必要的测试证据。

## 对话记录

- 这是默认动作，不需要用户重复提醒：每次完成开发、解释技术链路或回答 Agent/RAG/Go/数据库/前后端问题后，都要提炼核心内容并写入 `docs/conversations/` 或对应专题 Markdown；不能只在聊天中回答后结束。
- 记录重点是“问题、结论、实现链路、关键代码概念、验证结果、待办和未决风险”，不逐字复制完整聊天；如果一次对话同时包含学习问题和代码改动，学习笔记与开发记录应合并整理，保证后续能独立阅读。
- 在完成小任务后的追问、需求澄清、关键决策和用户确认中，如产生后续开发需要参考的信息，应及时整理为简洁的对话记录。
- 对话记录统一保存到 `docs/conversations/` 目录，按日期或主题命名，例如 `docs/conversations/2026-07-25-需求确认.md`，避免影响其他 `docs` 内容的目录结构。
- 记录应包含背景、已确认结论、待办事项和仍未确定的问题；不要原样复制无关的完整聊天内容。
- 用户关于开发、架构、Go、RAG、Agent、数据库、前后端实现等知识点的提问，即使没有产生代码改动，也要整理成简洁学习笔记，优先追加到当天汇总或对应专题记录。
- 每完成一个可验证的小任务后，及时更新开发进度：已完成内容、验证结果、当前阻塞项和下一步计划。
- `docs/` 为本地工作资料，默认不纳入版本控制。

## 学习式提问

- 用户提出技术问题时，先将问题改写为更清晰、上下文完整且可执行的版本，帮助用户学习如何更准确地提问；改写不得改变用户原意。
- 在给出问题改写后，再回答原问题。对于用户要求逐题进行的追问、测验或 grilling，问题改写应保持简短，避免打断逐题节奏。

## Git 工作流

- 每完成一个可验证的小任务或一组紧密相关的修改后，创建一次独立、可读的 Git 提交；不要将无关改动混入同一提交。
- 提交前检查 `git status` 与 diff，确认不会提交秘密、构建产物、本地文档或无关文件，并运行与改动相匹配的验证。
- 提交信息使用清晰的动词开头，准确说明本次改动，例如 `feat: add health endpoint` 或 `docs: define development workflow`。
- 保持 `main`/`master` 分支可用；较大功能、风险较高的修改或并行开发应创建简短、语义明确的功能分支，完成验证和审查后再合并。
- 不使用破坏性 Git 操作覆盖用户已有改动；提交、合并、变基或推送前先确认操作范围。

## 可用开发技能

按任务需要使用对应技能，并在使用前阅读其 `SKILL.md`：

- `tdd`：开发新功能或修复缺陷时，采用红—绿—重构循环，按可验证的小切片实施。
- `code-review`：完成实现后审查改动是否符合项目规范、存在明显代码坏味道，以及是否完整满足需求。
- `diagnosing-bugs`：排查复杂缺陷或性能回退时，遵循复现、最小化、假设、观测、修复和回归测试的诊断流程。
- `grilling`：每完成一个小任务、功能切片或阶段性成果后，主动向用户追问并确认：是否准确理解了当前成果、是否符合预期、是否存在遗漏或需要调整的方向；收到确认后再继续下一项工作。

若任务不匹配以上技能，遵循本文件的工程原则并使用项目既有工具完成开发与验证。
